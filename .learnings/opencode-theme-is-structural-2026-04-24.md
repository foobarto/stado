`opencode`-style TUI design is mainly structural, not just palette.

What mattered in practice:
- flat dark canvas
- inset panels with a single colored left rail
- much less border chrome
- assistant text mostly on the canvas, not inside a card
- sidebar as a quiet surface separated by a divider, not a boxed widget

Simply recoloring the old rounded-border layout produced the wrong result.
