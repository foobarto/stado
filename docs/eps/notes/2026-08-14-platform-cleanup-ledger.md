# Platform cleanup ledger — 2026-08-14

This is the durable working index for the architecture cleanup discovered while
finishing PR #257. It is not itself an EP and cannot override an accepted EP.
Normative decisions link to their owning EP; open interpretations and
inconsistencies are labelled as such so a future session does not mistake an
assistant's working conclusion for an operator decision.

The historical reason for treating drift seriously is recorded in
[the 2026-05-05 architectural reset](./2026-05-05-architectural-reset.md). A
long AI-assisted implementation and review cycle produced native
implementations behind plugin-shaped wrappers. EP-37 and EP-38 were the
correction, not merely a preference for one implementation language.

## Accepted decisions in this cleanup

- This is a strong pre-v1 corrective migration. Drive directly toward the
  accepted architecture rather than preserving drift-shaped interfaces. Remove
  obsolete imports, capabilities, wrappers, config, native application paths,
  and fallback writers instead of aliasing or deprecating them indefinitely.
  Preserve authoritative user data with explicit, auditable, one-way migration;
  that is data integrity, not API backward compatibility.

- [EP-63](../0063-plugin-defined-harness-artifacts.md) extends EP-53 with
  plugin-defined, schema-validated artifact `.data`, stable canonical kind
  namespaces, archived descriptors, and authenticated `stado_artifact_*`
  imports. It replaces the ambient `stado_memory_*` bridge.
- [EP-64](../0064-wasm-lifecycle-applications.md) extends EP-51 and EP-62 with
  persistent serialized WASM applications, lifecycle/event subscriptions,
  typed broker bridges, retained-agent/mailbox integration, scheduling holds,
  and the supervise migration.
- [EP-66](../0066-canonical-plugin-authority-and-application-placement.md)
  makes canonical runtime identity authoritative across secrets, state,
  artifacts, lifecycle, broker bindings, and audit attribution; it also marks
  every stado-owned native model application—including EP-60 guidance—as
  migration debt rather than a permanent exception.
- [EP-67](../0067-session-controller-and-application-selection.md) separates
  session-controller possession, exact application-local quality configuration,
  and security-significant artifact activation. A controller token or
  in-process UI callback is not operator-origin proof.
- Supervise is a quality gate, not a security boundary. A watchdog may still
  pause or stop the loop through host-enforced scheduling transitions.
- V1 budget enforcement uses token ceilings only across Stado and supervise.
  Provider-reported currency remains observational telemetry, not authority.
- Stale verdicts are asymmetric: discard stale approval, deliver stale
  correction as anchored advisory steering, and hold plus re-review a stale
  pause/stop before applying authority to current work.
- The existing `stado_ui_choose` (`ui:choice`), `stado_ui_print` (`ui:print`),
  and `stado_ui_render` (`ui:render`) imports are the generic operator UI bridge
  for WASM applications. The plugin owns wording and workflow; the host owns
  transport, sanitization, availability, and UI delivery. Durable authority
  requires broker policy or proof from a separately trusted presenter.
- Linux is the only supported platform target for the current project and for
  v1. macOS and Windows are not roadmap goals. Existing conditional code may be
  removed as part of cleanup and must not constrain the Linux architecture;
  its mere presence is not a compatibility promise.

## EP-2 stress test — working interpretation

**Question:** is “all tools as WASM plugins” too strict for a feature as large
and loop-integrated as supervise?

**Working conclusion:** do not weaken EP-2 for supervise. Clarify its scope and
pair it with EP-64 instead. EP-2 directly governs every model-facing tool; it
does not say that every line of native Go is forbidden. EP-37 and EP-38 supply
the broader split: stado provides generic capabilities and trusted execution
seams, while plugin authors own application policy. EP-64 deliberately applies
that split to lifecycle applications.

The reasons for the boundary, in order, are:

1. WASM gives plugins a deliberately sandboxed execution environment whose
   access to the host is limited to explicit capability-gated imports.
2. Keeping applications in plugin space keeps stado's core small and coherent,
   while allowing experimentation and innovation to proceed without repeatedly
   expanding the native product kernel.

Uniform dispatch, replacement, attribution, and auditing follow from those two
reasons. They are useful consequences, not substitutes for the original
rationale.

The strongest argument for a native supervise implementation is engineering
cohesion. It touches provider turns, tool dispatch, session identity, durable
ordering, child agents, budgets, and operator control. Native code avoids ABI
round trips, serialization, plugin crash recovery, and a larger broker surface.
It can be easier to debug and may be faster.

That is not enough to make supervise policy native. The same convenience
argument can exempt every sufficiently ambitious plugin. A native supervise
application would leave the plugin sandbox, gain implicit access to authority
and storage, expand and clutter the core with fast-moving application logic,
become harder to replace, introduce a second application model, and tempt
reviewers to move policy across the boundary one helper at a time. The
2026-05-05 reset is direct evidence that this failure mode is not hypothetical.

Supervise being a quality gate rather than a security feature does not change
where its implementation runs. The quality/security statement limits what
supervise claims about worker behavior. The WASM sandbox limits what supervise
plugin code itself can do to the host. Those are independent boundaries.

Use this review test:

1. If the code observes a broker-verified or kernel-observed fact, executes an OS/runtime action,
   serializes an authoritative transition, or enforces a ceiling that WASM
   cannot enforce on itself, it belongs in the host or broker.
2. If two plugins could reasonably make different choices from the same facts,
   it is application policy and belongs in the plugin.
3. A new native import should describe a generic capability independently of
   supervise. If its name or semantics encode a watchdog workflow, detector,
   verdict, or supervise state machine, the boundary is probably wrong.
4. The host may provide a protected input and validate a narrow output without
   owning the decision between them. Lifecycle callbacks are this shape.
5. Complexity and a fast-moving design space are reasons to make the sandboxed
   plugin application runtime adequate, not automatic exceptions from it.

Applied to supervise, native stado owns event ordering, authenticated anchors,
broker writes, scope checks, budgets, agent execution, mailbox delivery,
scheduling holds, pause/stop/cancel enforcement, and operator UI transport. The
WASM application owns contract wording, setup flow, detector and cadence policy,
evidence selection, prompts and schemas, reviewer/verifier orchestration, stale
verdict interpretation, correction policy, and model-facing tools.

An exception can still be proposed, but it should require a new accepted EP
that names the non-generic native behavior, explains why a generic primitive is
insufficient, and preserves a replaceable plugin surface. Implementation
convenience alone is not that case.

## Inconsistency register

| ID | Inconsistency | Current disposition |
|---|---|---|
| C01 | EP-62 claimed Implemented before PR #257 or v0.80.0 existed. | Corrected to Accepted; EP-64 extends it. |
| C02 | EP-30 claimed Implemented while stated harness-security work remains incomplete. | Corrected to Partial. |
| C03 | EP-33 claimed Implemented while responsive lane work remains incomplete. | Corrected to Partial. |
| C04 | EP-53 and the runtime hardcode memory/lesson fields and `stado_memory_*`. | **Corrected in source:** EP-63 generic broker artifacts use exact installed identity, signed schemas/projections, typed capability-gated imports, and no fabricated bundled learning owner or generic `legacy_id` field. Native memory/learn/adaptive prompt policy is deleted. The one official TUI lifecycle package owns both applications, but remains unsigned and unpublished, so EP-52/58/63/64 remain Accepted rather than claiming release completion. |
| C05 | Plugin runner and TUI open the legacy memory JSONL directly. | **Corrected:** ordinary code has no JSONL reader or writer. The sole historical path is lifecycle-only `artifact:migrate:legacy-memory-v1`: the broker owns the fixed rooted path, exact archive, identity/schema verification, session binding, bounded inert stages, destination expectations, final source recheck, and one logically atomic completion marker. Failure/quarantine leaves the source untouched; completion permanently fences rereads. |
| C06 | PR #257 implements six model-facing supervise tools in native Go. | Corrected: native supervise tools and their dispatcher are deleted. The official application owns three exact WorkerRun-scoped tools through signed-manifest metadata; release evidence is recorded in C34. |
| C07 | PR #257 opens the broker WAL directly from the TUI/supervise service. | Corrected: the native service is deleted and lifecycle applications use authenticated broker RPC/imports; the TUI retains only controller-owned generic operations. |
| C08 | Lifecycle hooks are usable from Lua but not signed WASM applications. | Corrected: EP-64 lifecycle declarations and one serialized persistent instance compose application tools, commands, hooks, events, ticks, and close in the TUI; unsupported surfaces fail closed. |
| C09 | `agent:fleet` was bundled-only and granted the whole agent surface. | Corrected: exact `agent:{spawn,list,read,send,cancel}` capabilities now gate each operation, the aggregate grants nothing, and retained work pins and verifies the admitted source before launch. |
| C10 | EP-38 says the no-native-tools migration is Implemented while documented Steps 8–10 and native carve-outs remain. | Corrected EP-2/38 to Partial; EP-66 and the shrinking native-surface guard own closure. |
| C11 | EP-39 D7 retains global active-symlink wording reversed by its later D12. | Corrected with a history entry and inline amendment that makes D12's canonical identity layout and project/per-user selection authoritative without erasing D7. |
| C12 | EP-44 retains stale claims about project MCP configuration in test/phase prose. | Corrected: the history, phase prose, and test strategy now agree that `mcp.servers` is always stripped and no project TOFU store shipped. |
| C13 | EP-45 refers to an EP-44 project TOFU gate that did not ship. | Corrected: project-skill `allowed-tools` is unconditionally inert; any future authority path needs a new accepted decision. |
| C14 | No accepted EP previously owned all of `stado_ui_choose`, `stado_ui_print`, and `stado_ui_render`; code comments point to temporary F9/F10 specs. | Corrected: EP-64 and the host-import reference own the UI bridge; production runtime comments cite them and an architecture guard rejects temporary slice labels. |
| C15 | DESIGN/PLAN and several older EP passages still describe macOS and Windows runners as product targets. | Corrected: EP-65 owns Linux-only current/v1 scope and explicitly extends the surviving EP-28/37/38 decisions; current docs, release metadata, installer, self-update, bundled fetches, and platform files are Linux-only. Older body text remains labelled decision history, not a live support promise. |
| C16 | Display alias `Manifest.Name` still namespaces secrets/state and several production paths synthesize local identity. | **Corrected:** loader-supplied `RuntimeIdentity` scopes secrets/state/audit across installed, bundled, override, TUI, headless, background, and CLI execution. Source-bound local identity is explicitly unstable. The fabricated native learn-kind owner is deleted; the remaining source-bound `NewHost` compatibility constructor is frozen shrinking debt. |
| C17 | EP-28 was both Partial and wholesale superseded by EP-38 despite surviving HOME/XDG decisions. | Removed the over-broad reciprocal supersession; EP-38 remains a scoped historical amendment. |
| C18 | EP-37 permissive defaults conflict with later broker/sandbox-first EP-50. | Added reciprocal scoped extension: EP-50 replaces default containment posture, not the plugin/application boundary. |
| C19 | Application/artifact bind, native event publication, and schedule projection accepted a raw session ID from any same-UID broker client. | Corrected with a hash-retained broker session-controller capability across bind/event/schedule/terminate/taint/child operations and exclusive lifecycle admission. It proves session control, not operator intent. |
| C20 | EP-50 language and new lifecycle prose risked calling a hostile-orchestrator-supplied context or TUI approval “unforgeable.” | Broker admission bindings authenticate broker-verified facts and ceilings to a guest, but an in-process TUI cannot prove a fresh operator gesture against a compromised orchestrator. Security authority needs predeclared policy or a separately trusted presenter; ordinary supervise setup is explicitly a quality workflow. |
| C21 | Durable application events initially exposed internal routing fields and allowed acknowledgement outside the binding's pending subscribed set. | RPC results now project only the public event shape and acknowledgements advance only over broker-verified pending subscriptions, while preserving idempotent retry. |
| C22 | EP-64 initially coupled supervise startup to an active artifact and therefore invited a false in-process “operator grant.” | EP-67 makes supervise contract choice exact application-local candidate selection with no authority transition; real artifact activation remains withheld pending broker policy or a separately trusted presenter. |
| C23 | Lifecycle applications bind at TUI boot but are not atomically rebound after session switch, fork recovery, or `/reload`. | Corrected: the TUI owns one root controller and an exact per-session peer, stages application composition transactionally, rolls back failed transitions, rotates peers on session changes, and reloads without duplicating the session binding. |
| C24 | The application API has pause/stop/holds but no successful-completion handoff. | Corrected: `session:complete` records an idempotent broker-owned completion, projects it into scheduler precedence, ends the worker loop successfully, and lets the application release its exact hold. |
| C25 | No production host publishes an exact-parent `agent.down` event. | Corrected: child terminal facts are durably published through a leased event context only to the authenticated parent session/generation, including distinct `agent_id` control handle and `session_id` immutable evidence coordinate, bounded scope/change, and C29 terminal metadata; switching sessions cannot relabel or drop an admitted child's event. This replaces the unsafe native PR #257 forwarding path. |
| C26 | ACP has no fleet, headless can compose legacy and lifecycle paths twice, and non-TUI surfaces do not uniformly route application commands. | Corrected for v0.80/v1: TUI is the sole supported lifecycle-application surface and owns exactly one persistent instance per canonical identity/session/generation. Run, headless, ACP, ephemeral plugin execution, and legacy background loading fail closed before partial/duplicate composition and name the configured application in an actionable diagnostic. |
| C27 | Native `/supervise` owns worker recurrence, but generic application commands cannot claim/start/finish a loop. | Corrected: signed applications can request/cancel a bounded durable `WorkerRun`; only a successful declared command may hand its exact run ID to the TUI for native CAS activation. One application owns recurrence, explicit policy may replace only an operator loop, stale ticks are generation-fenced, and completion/pause/stop/cancel terminalize durably below normal enforcement. |
| C28 | EP-62 followup routing and session continuation remained native-only. | Corrected: the broker now captures immutable 48 KiB-bounded input against the exact active application run, delivers a targeted mandatory-action event, accepts only `deliver`/`defer` through `session:input:route`, and derives one canonical deferred-task projection (`open` → `pending_continuation` → `continued`) without legacy task writes or marker scans. Completion validates the exact ordered deferred set and digest-checked idempotent receiver delivery commits before recurrence. Oversize/backpressure fail visibly without unsupervised fallthrough; startup, logical-session rebind, worker handoff, and cancellation retain a draft-preserving fail-closed ownership fence; source-specific failures survive reload and cannot clear one another. Terminal recovery preserves all unresolved queued/reviewing/ready originals, while native read-only open-task summaries keep cancelled/orphaned deferred work operator-visible. Capture, routing, and recovery provenance remains quality/audit data rather than security proof. Cross-generation automatic transfer remains explicitly separate C35. |
| C29 | Agent terminal reads omitted host-measured token usage and did not distinguish a valid result from a later cleanup diagnostic. | Corrected: terminal spawn/read results carry token-only host counters with an explicit completeness bit; cleanup is a separate bounded fingerprint and cannot erase valid final text. |
| C30 | A delayed reviewer spawn pinned the then-current parent head rather than the authenticated turn/event source. | Corrected: `source.at` accepts the exact authenticated application `turn_ref`; the host derives/authorizes its session and synchronously pins that immutable tree before fleet admission. Exact reviewer spawns start with fresh prompt context. |
| C31 | Supervise review and verification can outlive a leased hold. | Corrected in the official application: it renews and durably schedules the exact hold at half-TTL, replays renewal idempotently after restart, and requests a hold-independent fail-closed pause if renewal fails. Official release evidence is recorded in C34. |
| C32 | Native code marked every no-tool assistant turn as a completion candidate. | Corrected: `session.turn_committed` contains assistant/tool facts only. Supervise completion begins from the plugin-owned explicit completion tool and durable plugin state. |
| C33 | Native supervise's `gh`/shell risk parser is both incomplete and application policy. | Corrected: the parser and native supervise workflow are deleted rather than expanded. Generic signed tool identity, host registry mutation class, and outcome remain typed facts the plugin may review conservatively; security approval remains central tool/broker policy. Any finer effect taxonomy needs an application-independent EP. |
| C34 | Official supervise remains unsigned and absent from the ordinary installed lifecycle path. | **Closed for initial distribution:** offline-key-signed `supervise/v0.1.0` was published and clean-install verified. The v0.80.1 terminal audit then found its Go build embedded an absolute GOROOT, so immutable corrective `supervise/v0.1.1` pins Go 1.26.6, uses trimpath and an empty build ID, and byte-reproduces across distinct GOROOT locations. Its signed released hashes are WASM `f4ce486e4a05c4fba7e597874cfb747e55e94347642b93cd4e34a6234c4f8ab6`, manifest `b840c2799d942e7913d4883705b411a98d955b434abb5b1c6557fe338d243ae3`, and signature `10ed9552e8ae0b46adfcb3c71d66023a456424f2d1951351b1ba34c952db5856`. Fresh GitHub release bytes passed signature, digest, isolated install, and installed-package verification against the official anchor with released stado v0.80.0. The full plugin-owned PTY/conformance repeat against exact v0.1.1 bytes remains evidence debt and does not close C86-C88 or broader EP-62/64 gates. |
| C35 | An application-owned worker run and its journal/input state were scoped to one exact logical session, while automatic context recovery switched to a compacted child without transferring that authoritative scope. | Corrected with a native-only, two-phase logical-subject handoff for the whole broker application scope. The source controller reserves an exact direct child at an authenticated turn; the native client pre-stages the same no-follow 0600 recovery bearer under the child; and a broker-side canonical-lineage verifier gates the WAL commit. Commit preserves SessionID, generation, and every application WAL key while changing only the logical subject, rotating the controller/version, and invalidating bindings. Crash before commit leaves the source authoritative; exact prior-controller/bearer replay closes the lost-reply window across broker restart; unresolved outcomes fence ordinary input and never revive the source. Manual forks remain independent. The documented same-UID bearer-theft limitation is unchanged. |
| C36 | The first migration plugin accepted only `status` or `start <complete-contract-json>`, while the accepted feature requires an application-owned setup wizard, fresh baseline proposal/quality confirmation, and durable `resume`/`cancel`. | Corrected in the official application. `/supervise [start] [objective]` durably sequences the bounded wizard, spawns an idempotent fresh read-only baseline architect, strictly validates and renders constraints/non-goals/criteria/ordered plan/DoD/verification/risks, and proposes a candidate plus WorkerRun only after quality confirmation. `status`, exact interrupted-run `resume`, and CAS `cancel` reconstruct from broker projection and never revive stopped/cancelled/completed work. Command/UI provenance grants no security authority; there is no native wizard fallback. Official release evidence is recorded in C34. |
| C37 | A full Stado process restart created a new broker session/controller, leaving the prior logical session's durable application binding, worker run, holds, events, cursors, and pending input unreachable even though their WAL records survived. | Corrected: the broker now mints an expiring, parent-controller-bound reservation before first durable session creation; the native layer saves its opaque recovery bearer in a no-follow 0600 credential file before commit. Restart adoption preserves the exact SessionID/generation/application WAL scope, rotates the live controller and bindings, and rejects live, cross-repo, cross-parent, replayed, terminated, or malformed claims. The stable recovery bearer avoids a one-response rotation crash window. This is local-user bearer protection, not authentication against a malicious same-UID process. |
| C38 | Application commands inherit the lifecycle callback's 60-second maximum even when a signed command intentionally invokes the generic interactive UI bridge, forcing a real setup wizard into one opaque JSON field or a sequence of fragile re-invocations. | Corrected: signed `commands[].timeout_ms` defaults to the lifecycle timeout and is independently capped at 15 minutes. It changes only wall-clock cancellation for the serialized command callback; it grants no capability, makes no surface interactive, and leaves hook/event/tick limits unchanged. The supervise plugin can sequence ordinary bounded `stado_ui_choose` calls instead of restoring native widgets or inventing a bespoke form import. |
| C39 | C27 initially called `worker.get`, `worker.activate`, and host cancellation through the ordinary application binding and relied on the absence of corresponding WASM imports to call them “native-only.” A hostile orchestrator holding that bearer could still select them, so the broker did not enforce the claimed boundary. | Corrected: all three operations use explicit session-controller-authenticated RPCs. The controller authorizes the native action and the exact lifecycle binding selects the broker-derived plugin namespace/generation; the guest operation map rejects them, and an AST guard prevents hidden-import reasoning from becoming an authority boundary again. |
| C40 | A long-running signed application command could still race an ordinary prompt into the provider before the command finished its durable worker-run handoff. | Corrected: the fence remains active across the signed callback and the asynchronous native lookup/CAS activation or cancellation. Ordinary non-slash submission remains visibly in the editor and cannot start a provider turn; another application command, reload, or session transition also waits. Explicit slash controls remain available. |
| C41 | A replayed successful application command could observe an already-active broker WorkerRun but fail to restore local recurrence, while worker lookup/activation errors were transient notices that released the prompt fence. | Corrected: exact broker-active state reconciles once when no local owner exists; lookup/activation errors and non-active replies retain a distinct retryable ownership fence. A different local owner is never overwritten or used to cancel the broker-active run implicitly, and `/loop` cannot start a new competing owner while recovery is unresolved. Generation-fenced replay, draft preservation, and independent C28 failure sources are regression-tested. |
| C42 | Conversation persistence claimed a partial trailing JSONL write was recoverable, but the next append could concatenate directly onto that fragment and the decoder stopped before every later valid record. C28 receiver-delivery commit would then be able to outlive an invisible appended message. | Corrected: in-process conversation appends serialize, preserve and newline-frame a crash-shortened tail before the next complete record, and decoding skips that invalid fragment while continuing to later valid frames. Matching delivery evidence rejects unknown fields, trailing JSON, duplicate IDs, invalid role/batch shape, and byte/schema/digest mismatches; partial-tail → append → reopen → idempotent replay is covered. |
| C43 | The development supervise plugin derived a setup/run ID from an in-memory lifecycle callback sequence that resets on module rebind, so an identical later setup could reuse a terminal broker WorkerRun identity. | Corrected: every new workflow uses 128 bits from WASI's cryptographic random source. Callback sequence remains non-authoritative instance-local ordering, and regressions prove an identical post-rebind setup cannot reuse a terminal run identity. |
| C44 | Several C36 setup/baseline/worker transitions mutated in-memory plugin state before the journal append that made the transition durable, without consistently rolling back or marking the unjournalled broker result for retry. | Corrected across setup, rejection/cancel, baseline terminal/ready, artifact selection, worker request/resume/cancel, review, and completion boundaries. The plugin validates exact journal acknowledgements; an ambiguous append invalidates memory and forces projection refold; pre-append state is restored where retry is local. Stable effect keys make committed broker results replay rather than duplicate, and restart/rebind crash regressions cover both sides of each boundary. |
| C45 | `stado_agent_spawn` could admit an asynchronous child and lose the reply when a lifecycle callback was cancelled or rebound before the plugin journaled the returned child ID. Retrying setup or review could then create an indistinguishable duplicate watchdog. | Corrected: a bounded guest idempotency key is scoped to the authenticated plugin/session/generation and bound to a digest of the exact normalized spawn request. Concurrent or post-rebind replay in the same live host returns the same Fleet child; reuse with different input fails closed, generation changes isolate it, and failed pre-admission attempts remain retryable. The map is intentionally process-local because Fleet children terminate with the process. The supervise plugin journals deterministic intent keys, reuses them across callback/module rebind, and can adopt only the exact authenticated ownership when an `agent.down` event wins the reply-journal race. |
| C46 | The migration plugin had the core watchdog/verifier flow but not the full EP-62 failure policy: bounded reviewer retry/backoff, the ten-consecutive-failure pause, live-review retry, correction follow-up, and bounded worker/watcher handoff were incomplete. | Corrected in the official application: event reviews use three fresh attempts and pause after ten consecutively exhausted triggers; success resets the streak; live review retries indefinitely with a durable 500 ms exponential delay capped at eight seconds; every correction forces a next-turn review and pauses after three failed corrections; and a typed 16-item-per-field handoff reaches each fresh reviewer. Attempt counters, retry deadlines, timers, child ownership, handoff, and correction state are journal-replayable with attempt-scoped idempotency. Valid verdicts remain independent of cleanup diagnostics. Official release evidence is recorded in C34. |
| C47 | Generic immutable operator-input routing is implemented, but the migration supervise application did not subscribe to, asynchronously classify, route/defer, or order continuation inputs. | Corrected in the official application: it durably records intent, uses C50's exact claim before ACK, starts a fresh pinned read-only classifier with stable process-restart replay, and routes only from matching authenticated terminal facts. Spawn/read failure retries within a bounded deadline then conservatively defers; no lexical fallback exists. Capture invalidates a racing completion before obsolete-child cancellation/hold release, exact deferred IDs retain broker order, and a 65th defer requests typed fail-closed pause plus native terminal recovery rather than dropping or relabelling the original. Callback-loss, rebind/restart, completion race, multi-input, failure, cleanup, ordering, and overflow regressions are green. Official release evidence is recorded in C34. |
| C48 | The accepted setup can choose reviewer/verifier provider and reasoning controls, but generic `agent.spawn` exposed `model` without a separately bounded provider, thinking mode/budget, or effort contract. | Corrected: optional provider/model, `auto|on|off` thinking with token-only budget, and bounded reasoning effort require both exact `agent:spawn` and non-standalone `agent:spawn:configure`. Provider construction/credentials stay native; exact resolution, token bounds, unsupported controls, forwarding, and capability attenuation are regression-tested. The signed supervise source declares both caps, restores separate watchdog/verifier advanced choices, bounds provider/model/budget/effort, and maps every spawn field byte-exactly across rebind. |
| C49 | Broker pause consumption terminalized an application WorkerRun as `interrupted`, but no generic transition could continue that accepted workflow even when its application state and holds were recoverable. | Corrected: `session:worker:resume` CAS-records an exact `resume_requested` transition without changing run identity, prompt, journal, input, deferred ownership, or WAL order. A distinct successful command result triggers controller-authenticated lookup and resume activation; stale CAS, later stop/completion, racing pause, conflicting owners, and any unexpired aggregate session hold fail closed without changing the request. Hold release/expiry permits exact retry. Cancelled, stopped, and completed runs never resume. Core replay/restart, RPC/import, native capability, TUI handoff/recovery/conflict, and own/cross-plugin/expired/raced hold regressions cover the contract. |
| C50 | C28 initially required an `operator.input.queued` event to be routed before its mandatory cursor could advance. EP-62 requires a fresh asynchronous watchdog to classify that input, but the watchdog's later `agent.down` shares the blocked cursor, so a correct plugin would deadlock or be tempted to replace the accepted policy with a lexical classifier. | Corrected: `stado_session_input_claim` uses the existing `session:input:route` capability to CAS an exact queued input/run/version into durable `reviewing` with a bounded untrusted review correlation ID. Only that transition permits the queued event ACK; rebind projects the same claim for exact idempotent child-result replay, and only the same binding/version/review ID may route it to `deliver|defer`. Reviewing remains pending for queue bounds, blocks completion, is an aggregate provider/tool scheduling fence, and joins queued/ready terminal recovery without changing original text or order. There is no native review timeout or classification policy. Core WAL/restart/idempotency/cross-scope/completion/capture-race/recovery tests, capability/RPC projections, native schedule precedence, and TUI turn-boundary delivery regressions cover the primitive; fresh-watchdog invalidation, hold settlement, and classification remain signed supervise-application policy. |
| C51 | Accepting a repository owner's published plugin anchor stored only its fingerprint, then installation asked separately for the same signer public key. The documented one-decision trust flow therefore failed on first install and could partially pin one store without the other. | Corrected: remote install retains the exact fetched key as an uncommitted candidate, verifies source/package identity, digest, signature, dependencies, and CRL, then atomically records signer key, owner association, and package floor in one trust-store transaction. Failed verification leaves no trust; unified cached/offline use, first sight, mismatch, and rotation refusal are covered. The split legacy fingerprint store is deleted rather than treated as authority or migration input. |
| C52 | Rollback protection stored one `LastVersion` per signer, so independently versioned official plugins sharing one offline key advanced a global floor and rejected one another as rollbacks. | Corrected: rollback floors are keyed by signer plus canonical package/source namespace. Display `Manifest.Name` is never used to derive a trust namespace. The legacy signer-global value remains inert audit evidence and is neither consulted nor migrated into package authority. Two-package rollback and inert-legacy regressions are green. |
| C53 | Remote source revision and manifest package version were conflated. A tag mismatch was not rejected at install, while a full-commit source could never equal a semver and silently fell back to unstable local-path runtime identity. | Corrected: lock/runtime identity carries logical identity, exact `source_revision`, signed `package_version`, and commit evidence independently. Tagged selectors must match the signed version; SHA locks require package-version evidence; anchor/ref/version mismatches fail closed rather than falling back to local identity. Tagged mismatch, SHA, lock reopen, legacy-lock, and invalid-source regressions are green. |
| C54 | Every plugin subdirectory in the official monorepo asked GitHub for one repository-wide latest release. A release for one package could make every other package fetch the wrong tag or stale raw assets. | Corrected for v0.80: GitHub discovery selects only `<subdir>/vX.Y.Z` releases or repo-wide releases carrying the complete exact package-prefixed asset set; siblings and ambiguous flat assets are ignored. GitLab monorepo latest is explicitly exact-pin only. Pinned fetch prefers the package tag and records the actual source ref; cross-package selection regressions are green. |
| C55 | The development supervise plugin hardcoded its future official artifact namespace. A correctly local-signed installation receives a `local://sha256/...` runtime identity and therefore could not read back its own proposed contract without forging an official lock or granting wildcard reads. The broker verifier also rejected every non-EP-39 identity before inspecting the signed installed package. | Corrected: exact signed `artifact:{read,observe}:self#<declared-local-kind>` grants are resolved independently by the host and broker from the authenticated runtime identity. Wildcard/undeclared self forms fail, and unresolved pseudo-kinds never reach broker storage. Broker admission enumerates only configured install roots, rejects symlinked package directories, recomputes the exact remote-lock or local install-path identity, then rechecks the source-scoped signer pin, manifest, and WASM bytes. Supervise now queries `self#supervision-contract`, so isolated ephemeral-key integration exercises the real identity boundary without pretending to be official. |
| C56 | `/supervise` is still a native reserved/static slash command with a lifecycle-app collision exception and fallback. Alias expansion can shadow an admitted application command before dispatch, while application-to-application collision order depends on config order. | **Corrected:** the native command, picker, workflow/service, model tools, evaluator, reserved/static palette row, exception, and fallback are deleted. Generic application admission resolves command ownership deterministically before exposing it, rejects application/application and application/native collisions atomically, and lets an admitted owner precede stale aliases/skills without special-casing `supervise`. The palette discovers the exact application command dynamically and preserves its full arguments. Absence or callback failure never falls back. Focused boundary, alias/skill, palette-argument, dynamic-discovery, collision, and native-fallback architecture tests are green. |
| C57 | Eight native registry/plugin meta-tools and their result interceptors make Go own model-facing discovery, activation, and unloading even though signed manifests already describe tools. DESIGN still called this a native discovery kernel. | **Corrected:** the eight registrations/types, `IsMetaTool`, direct CLI bypass, result parsers, and native debt allowances are deleted. `stado_registry_catalog` exposes only capped loader-bound facts and a complete-projection digest; `stado_session_tool_surface_apply` validates a whole digest-fenced batch against the exact registry/config/session ceiling before atomic mutation. Caller-owned and every persistent-lifecycle tool are excluded. Search, formatting, category policy, describe-and-activate, and exact unversioned source grouping live in the explicit-opt-in official `tool-registry` WASM package. Focused, race, ABI, architecture, and reproducible-build checks cover the cutover. |
| C58 | `skills__load` and prompt builders turn model-selected plugin content into a synthetic user-role message, falsifying provenance and requiring hardcoded interception across every execution surface. | **Corrected:** native registration, system-prompt listing, exact-name autoload/result interception, body trimming, and synthetic user-role injection are deleted. Exact `context:resource:catalog:skill` / `context:resource:open:skill` capabilities gate strict digest-fenced generic facts. Native admission mechanically omits model-hidden or rendered-over-128-KiB skills, keeps project tool declarations inert, and intersects at most 64 unique persona names with immutable exact filtered-registry/session ceilings; operator invocation retains its separate 1 MiB loader allowance. The explicit official `skills` WASM source owns search, matching, formatting, open, and generic atomic allowed-tool activation; search returns opaque IDs and load requires one exact current-catalog ID, so duplicate display names remain usable and names never select authority. Loaded Markdown remains a labeled ordinary tool result across run/TUI/headless/ACP/subagents. Generic signed `tools[].capabilities` attenuation ensures search cannot inherit its sibling's open/surface-edit authority: every ordinary tool must preserve field presence (`[]` is zero; omission rejects), admission requires an exact unique package subset, selected-tool Host/risk/broker binding and nested dispatch use that subset, while persistent lifecycle tools must omit the field and honestly retain their shared package Host. Operator `/skill`, slash, and `--skill` remain truthful native gestures. Focused/race/ABI/architecture and reproducible unsigned build checks cover the cutover; publication still requires the real offline key. |
| C59 | EP-54 was marked Implemented, but `memory__research` and `session__research` used a private native provider loop, native corpus adapters, and direct WAL access rather than the specified broker-created read-only child using the ordinary agent loop. | Corrected and completed in source. Generic exact-tool-bound evidence catalog/search/open/validate imports now derive authenticated artifact or current-lineage session scope, enforce durable concurrent call/open/byte caps, store immutable read receipts, and mechanically validate exact direct-child citations without claiming entailment. The explicit unsigned official `research` WASM source owns both outer workflows and six exact `agent_child_only` helpers; ordinary parent/no-narrow/unrelated-child projections cannot see them. Native registrations, private provider loop, corpus adapters, and CLI/TUI direct-WAL research bridges were deleted. Release-key signing/publication remains release work. |
| C60 | Native tasks used a JSON state store, model tool, TUI picker/key/static command, MCP registration, and default autoload even though EP-63 provides plugin-defined broker artifacts. | **Corrected:** the explicit unsigned official `tasks` lifecycle application owns dynamic `/tasks`, one Do-only `application_session` model tool, generic `stado_ui_choose` UX, and global plugin-defined `task` candidates with live/deleted selector tags and versioned logical tombstones. Generic digest-fenced pagination is bounded to 50 per page and 1,000 live heads; broker-namespaced mutation keys converge across sessions/restarts, explicit create keys fence reply-loss retries, and no-op edit retry creates no version. The one-way importer is rooted only at `cfg:state_dir/tasks`, fails closed above the exact 16 MiB read ceiling, proposes with digest-bound keys, re-queries immutable ID/version/data proofs, verifies a byte-exact digest archive, rechecks concurrent source change, then replaces `tasks.json` with a strict receipt. No dual write exists. The native store/tool/picker/key/static command/MCP registration/default autoload and interceptors are deleted; architecture and application-projection guards prevent fallback. Reproducible unsigned source/unit/race/vet/WASI checks are green; offline signing/publication remains release work. |
| C61 | EP-60 native guidance composes application policy and wording in Go, opens WAL state directly, and is injected across run/TUI/ACP/headless/subagents. | **Corrected in source:** `session:context:read` resolves an opaque application binding and exposes only bounded signal/child/unread facts; another application's learn/review journal is deliberately excluded. TUI `pre_llm` adds explicitly untrusted current-input/fast-context facts and passes its live tool-surface controller to the exact registry catalog. A distinct `lifecycle:contribute:pre_llm` appends at most 16 KiB and cannot deny/model/history-mutate; failure-open callback/decode faults contribute nothing even under global Lua fail-closed. Canonical app order and decide→contribute chaining are covered. All native policy and non-TUI composition are deleted. Reproducible unsigned official `guidance` source owns thresholds, classifiers, ordering, and wording; offline signing/publication remains release work. |
| C62 | The MCP server registered stado's native `llm.invoke` application; this was not an externally implemented MCP tool and the old host import mixed provider authority with application prompt/output policy. | **Corrected:** `internal/tools/llmtool`, its native registration, the MCP-only persona flag, `stado_llm_invoke`, and the optional/default `llm:invoke` capability are deleted. Exact `provider:invoke:<positive-tokens>` gates a strict `stado_provider_invoke` primitive. Native code constructs the operator provider, retains credentials, binds loader-authenticated identity, propagates cancellation, atomically reserves and commits cumulative input+output tokens, and returns bounded versioned facts with separate semantic/cleanup fingerprints; cache counters remain factual subdivisions and USD is absent. Request shaping, output policy, and the model-facing tool live in reproducibly built unsigned `llm-invoke` source staged in the official plugin repository for explicit opt-in/signing at release. Bundled auto-compact uses the same primitive. Focused, race, ABI, MCP, architecture, and reproducible-build checks cover the cutover; only the adapter for operator-enabled externally implemented MCP tools remains native. |
| C63 | Bundled core WASM tools were manually registered through 33 Go constructors that owned schemas, descriptions, classes, capabilities, and duplicate metadata; `wasmFamilies`, `runtime.use_wasm`, migration wrappers, and a dead bundled wrapper preserved a second contract source. | Corrected: every core module now owns a source-adjacent strict manifest; one generic embedded-manifest loader validates source/module binding, exports, capabilities, duplicate names, and contract shape, then returns defensive copies. Runtime registration is one generic adapter for 33 manifest-derived contracts. The migration switch, family tables, manual constructors, wrapper metadata, compatibility aliases, and parity scaffolding are deleted, and the architecture guard requires those debts to remain at zero. |
| C64 | The shrinking native-surface guard exempted Go-owned bundled contracts and scanned only a subset of exact-name interceptors; it missed `llm.invoke`, guidance prompt composition, AgentLoop/model interceptors, duplicated metadata, and migration scaffolding. | Corrected: exact shrinking baselines cover the discovered stado-owned registrations, skill/agent interceptors, guidance policy/composition seams, duplicated metadata/consumers, and deletable empty native registry. Bundled migration scaffolding and native supervise, registry, skills, research, guidance, provider-application, and tasks debt are now zero, with explicit guards preventing their native fallbacks from returning. The only permanent allowance is one exact external `mcpbridge.MCPTool` adapter. |
| C65 | Merely configuring the migration supervise application could deny ordinary work before a run existed, and cancelled or successfully handed-off workflows could remain permanently fail-closed. | Correct in the signed application: no selected workflow is dormant and acknowledges events; setup, active review, and incomplete completion handoff retain their intentional quality fence; cancellation becomes dormant only after exact child/run/hold cleanup; successful completion becomes dormant only after broker completion handoff and exact hold release. Add pre-start, post-cancel, post-handoff, reload, and crash-boundary regressions. Native scheduling facts remain the enforcement authority. |
| C66 | Admitted lifecycle-application tools were registered but not projected into an owned worker turn. Native supervise names received a hardcoded exception, while generic state-mutating application tools disappeared in Plan mode. | Corrected: a lifecycle manifest may explicitly opt each tool into its exact broker-owned WorkerRun with a closed, signed `application_worker.plan_visible` boolean. Every persistent-application tool is excluded from ordinary, other-application, and BTW surfaces; Do receives only the exact owner's opt-ins and Plan receives only its explicit Plan subset. Global enabled/disabled policy and session disables remain ceilings. Admission preflights every collision before changing the registry, permits replacement only of the same package's ordinary adapter, and rejects duplicate persistent instances. Provider turns bind names to exact adapter pointers; dispatch rechecks the active run's owner, session/generation, version, WAL anchor, status, and registry instance, then carries that pointer through the ordinary executor audit/hook/sandbox path so rebind cannot change name meaning. Manifest, ownership, mode, ceiling, collision, rebind, terminalization, and exact-dispatch regressions are race-green; C56 completed the native deletion. |
| C67 | Supervise completion requires current passing verification facts, but no generic application can request the operator-configured Verify Work suite and the current TUI boundary publishes none. Baseline prose must never become executable authority. | Corrected with `session:verification:request` / `stado_session_verification_request`. The guest supplies only exact run/version/pending-event correlation and idempotency; broker derives the immutable pending turn anchor and claim waits for cursor ACK. Controller-only native execution resolves `[verify].commands`, bypasses only the application's hold, rejects hook mutation, fences session/composition/tree changes, and returns strict ordered outcome/digest facts with exact signed trace/tree and terminal-WAL evidence. No command/output plaintext or authority fields enter durable facts. `no_suite` is neither pass nor fail; worker terminal state wins; reply-loss is identical; crash-after-command/before-terminal-WAL remains explicitly at least once. Focused broker/RPC/runtime/TUI and race regressions cover malformed WAL/facts, source/terminal evidence identity, hold/completion, hook rewrite, stale anchor, rebind cancellation, and concurrent pumps. |
| C68 | The migration application recorded pivot requests and preferences but never resolved `PendingPivot`, updated the candidate contract, or re-anchored plan state. | Correct in the signed application with a strict structured replacement. A watchdog may approve only a configured plan-only pivot; all other plan pivots and every contract-changing pivot require an explicit `/supervise resume` quality confirmation. Exact `artifact:edit` CAS, selection journalling, plan-version increment, re-anchoring, completion-verification invalidation, and evidence preservation are application policy. TUI confirmation is not security authority. |
| C69 | C53 separated a source ref from the signed package version, but semver installs still recorded only the tag text. EP-39 requires the dereferenced full commit and loud tag-rewrite detection; a moved tag or replaced same-tag release package could silently overwrite its lock entry. | Corrected: semver refs resolve through bounded GitHub/GitLab source APIs to a lowercase full commit before fetch and are confirmed again afterward; annotated tags dereference to their commit. Locks persist ref, resolved commit, package version, canonical manifest digest, WASM digest, and anchor. Existing exact identities are compared before trust/install mutation; moved mappings and same-commit package replacement refuse by default, and only the dedicated `--accept-tag-rewrite` action permits an out-of-band-verified replacement. Full-commit identities are metadata-independent exact pins. Resolution, package-rewrite, replay/refusal/acceptance, legacy/duplicate lock, immutable-pin, and bounded-cache regressions cover the contract. |
| C70 | Remote plugin downloads created and wrote directly into a persistent same-UID-writable cache with `os.MkdirAll`/`os.Create`, which followed pre-planted directory or file symlinks and silently truncated oversized artifact, release-metadata, and anchor-key responses. | Corrected: cache creation walks below the user/XDG anchor without symlinks; every response uses explicit `limit+1` rejection; artifacts are atomically replaced through a no-follow directory-root resolver. A partial/oversized response changes no cache file, metadata is never parsed from a truncated prefix, and a symlink cannot redirect directory creation or file writes into operator state. Focused regressions cover the body and final-target invariants; the shared resolver suite covers parent-component refusal. |
| C71 | EP-39 and the runtime authority model make `Manifest.Name` display metadata, but install destinations, configured plugin IDs, update/remove selection, and lifecycle activation still begin from the flat `<name>-<version>` alias. Two signed packages from different canonical source namespaces can declare the same display name/version and collide or replace one another before exact lock identity is consulted. | **Corrected:** installs use a host-derived `remote-<sha256>`/`local-<sha256>` key over a complete provenance record. Remote reopen requires the exact store-keyed lock row; local reopen retains canonical original source plus signer and explicitly documents the same-UID limitation. Discovery rejects legacy flat entries and symlinks. Activation, lifecycle/background/override/headless/TUI selection, broker verification, update, remove, GC, info/doctor/reload, and installed tool paths use authenticated canonical identity/store keys; aliases fail on more than one matching installed row across roots. Retained rollback/tag-rewrite packages retain exact lock rows, and explicit local rebuilds select the newly verified row. |
| C72 | The custom `stado plugin bundle` payload carries only guest manifest/signature/WASM bytes, reconstructs module identity from `Manifest.Name`, and uses a self-generated unpinned bundler key. After C63 its appended modules are not part of the manifest-derived runtime registry, so the documented feature is simultaneously identity-unsafe and non-functional. | **Corrected:** the unowned pre-v1 CLI, appended payload codec, startup loader, mutable bundled registry, unsafe verification bypass, and false documentation are deleted. Reintroducing portable custom binaries requires a Standards EP defining source-exact identity, a pinned bundle signer, manifest-derived registration, update/revocation semantics, and collision behavior. |
| C73 | Plugin `requires` entries still name dependencies by the guest-controlled display field `Manifest.Name`. Ambiguity fails closed, but a unique alias still decides dependency identity and therefore conflicts with EP-39/C66's canonical-source authority rule. | **Corrected:** the unused pre-v1 manifest field, parser, installer solver, tests, and documentation are deleted. No shipped/official manifest used it. A future dependency contract requires an owning EP for canonical sources, active-version semantics, scope precedence, and failure behavior; display aliases cannot be revived as dependencies. |
| C74 | Compatibility trust and uninstalled verification helpers still derive a package namespace from `Manifest.Name`, retaining a second identity regime after installed execution moved to source-exact verification. | **Corrected:** name-derived compatibility namespaces and their trust/check APIs are deleted. Every rollback mutation receives an explicit source namespace; local `plugin verify <dir>` derives it from the canonical source path and uses the exact package-scoped verifier. The legacy signer-global field is inert audit evidence only and is never consulted or migrated into source authority. |
| C78 | Installed tools from distinct authenticated source namespaces are grouped through unordered maps and registered by unconditionally overwriting the model-visible name. Package iteration therefore decides which signed implementation owns a collision, and a later global package can replace a project-priority tool. | **Corrected:** active packages are authenticated and collected before registration. Every package participating in a cross-source collision is disabled, and any package colliding with an already-registered bundled/native/external owner is rejected atomically before any of its tools register. Neither root, manifest, nor map order selects a winner. Only the explicit operator-owned `[tools].overrides` path may replace an existing tool; reversed-order and partial-package regressions cover both collision classes. |
| C79 | Missing, corrupt, empty, oversized, unreadable, and stale active-package markers collapse into the same empty result. Runtime silently falls back to the highest installed version, while GC parses the marker separately and may delete around a broken pin. | **Corrected:** one bounded parser distinguishes absence from invalid presence, validates exact store-key syntax, and returns stale membership as an error. Runtime selection and GC share it; empty, malformed, oversized, and stale regressions prove corruption cannot become an implicit upgrade. |
| C80 | The same exact package/store key can exist in both project and global roots, but management resolution treats both rows as an unresolvable duplicate even when the caller supplied the exact store key. Conversely, namespace aliases may span several retained versions although update/remove need set semantics rather than one arbitrary row. | **Corrected:** management accepts explicit `project:`/`global:` prefixes that select one root without changing authenticated identity. Unscoped duplicates remain ambiguous. Update targets the active row in that scope; remove resolves one exact row then removes its source-namespace set only in that scope. Info, verify-installed, use, doctor, and reload share the resolver; identical cross-root store rows and scoped removal are covered and documented. |
| C81 | GC removes source-keyed store directories without reconciling their exact lock rows, active markers, or retained accepted-tag-rewrite history. The lock therefore ambiguously mixes installed-set state with provenance history. | **Corrected:** plugin-lock is immutable provenance/tag-continuity history, not a live installed index. Host receipts are live admission: remove/GC revoke selected exact receipts before deleting bytes, keep active rows, and retain lock history so reinstall still detects a moved tag. Receipt, byte, active-pin, and lock-retention regressions cover the chosen fail-safe ordering. |
| C82 | Installed-plugin ABI preflight checks `stado_tool_<ToolDef.Name>`, while execution correctly invokes `stado_tool_<ToolDef.ExportName()>`. A signed tool whose model name and WASM export differ is therefore rejected even though the runtime could invoke it. | **Corrected:** ABI preflight and dispatch both derive `stado_tool_<ToolDef.ExportName()>` from signed manifest data. A distinct model name/export regression proves no name-derived symbol is required. |
| C83 | `min_stado_version` is displayed and documented as an install compatibility gate but has no production install or runtime-reopen enforcement. A package requiring a newer host/ABI is admitted and fails later in less legible ways. | **Corrected:** install checks the signed stable-semver minimum before continuity/trust mutation, and every installed runtime reopen checks it again. Older, malformed, development, and prerelease host identities fail closed when a minimum is declared; final supervise proof must use a host built as v0.80.0. |
| C84 | The source-keyed store rejects every non-store directory, but the still-live legacy anchor migration reads and creates `plugins/anchor-trust/` under that same root. One old metadata directory can therefore make all otherwise valid installed packages undiscoverable. | **Corrected:** the split AnchorTrustStore and all migration/dual-delete callers are deleted. Unified trust is the only owner binding; stale `plugins/anchor-trust/` receives an explicit retired-state diagnostic instead of being interpreted or silently skipped. |
| C85 | Installed-package trust policy is not uniformly rechecked. Lifecycle admission and explicit CLI execution use the full verifier, but ordinary auto-registered model tools and some ABI/broker verification paths use only the host receipt/signature/digest check and therefore omit configured dynamic CRL/Rekor policy; invocation may recheck only WASM bytes. | **Corrected:** the full installed verifier is now the only production caller of the low-level receipt/source/signature check. Registry collection and every installed `Tool.Run`, lifecycle/background and headless execution, overrides, broker artifact admission, ABI preflight, TUI plugin dispatch, verify-installed, and CLI tool execution share its digest, exact receipt/source, host-version, signer, CRL, and Rekor policy. A post-registry receipt revocation regression proves a cached adapter cannot execute through stale admission. |
| C86 | After deleting native memory review, no production surface can consume an operator-origin grant for a fresh artifact candidate; an application command or UI choice is only workflow input. | **Open memory/learn package-release gate (not a v0.80 supervise-release gate):** the staged official memory/learn package is strictly candidate-only. Fresh activation remains unavailable until a separately trusted, predeclared EP-59 presenter reloads and presents the broker's exact candidate, then commits one actor-bound, version/digest/scope-exact, reply-loss-idempotent grant-issue/consume/activation transaction. A guest import, controller token, ordinary TUI callback, `stado_ui_choose`, or application-rendered prose cannot become activation authority. Historical approved records migrate active only to preserve authority already granted by the retired workflow. |
| C87 | Provider invocation has no durable idempotency key, so a crash after provider success but before the review result is journaled cannot prove whether a charge occurred. | **Explicit limitation:** the official learn source journals intent before invocation and refuses to repeat an ambiguous intent. Durable results resume receipt-bound proposal materialization without another call, but provider reply loss remains an honest failed/ambiguous outcome until a generic durable provider primitive exists. |
| C88 | A bounded session projection can omit old learning journal entries needed for restart recovery. | **Fail-closed by design:** the lifecycle application scans unfinished durable results before selecting new evidence, records immutable receipt IDs and provider/model/usage facts, and resumes partial proposal materialization. If the bounded projection reports truncation across the relevant review history, review recovery stops rather than repeating the provider or claiming restart safety. |
| C89 | The official guidance consumer and current documentation still expected learn-review facts after the generic session-context projection deliberately stopped exposing another application's journal. Unit tests fabricated the obsolete fields, so the advertised completed-review suppression could never run against the real host ABI. | **Corrected:** guidance consumes only the broker's actual signal/child/unread snapshot and makes no review-completion or suppression claim. DESIGN, EP-60/64/66, the adaptive-context article, no-internal-tools guide, ABI/reference text, changelog, and package README now state that application-private review state is excluded. A canonical `session-context-facts/v1` fixture is copied byte-identically between stado and the official package; both producer and consumer strict-decode it, validate its digest, and fail on schema drift. |
| C90 | The repeated final audit found that PLAN still listed completed orchestration, native-tool, tasks, manifest, distribution, and platform cutovers as future work; current product docs advertised unimplemented adaptive shadowing, removed native/guidance debt, nonexistent cross-platform source remnants, and cost caps; EP-28 pointed at deleted rollout tests without a current disposition; and EP-46 still treated USD cost as budget authority. | **Corrected:** PLAN is again a forward-only ledger with the exact source checkpoint and remaining conformance/review/sign/publish gates. README, DESIGN, the threat model, sandbox/doctor/slash references, static page, adaptive-context article, and no-internal-tools guide distinguish source-complete primitives from unsigned or unimplemented applications, Linux-only scope from removed targets, and token enforcement from cost telemetry. EP-28 is Implemented with its surviving UserConfigResolver/Fedora proof and historical paths labelled; EP-46 is token/turn bounded with cost observational. `TestCurrentDocsDoNotRegressCompletedCutovers` freezes the confirmed stale phrases. |
| C91 | The closing status sweep found EP-0002 marked Implemented with a v0.80.0 history entry but no top-level `implemented-in`, while the EP process still called release metadata optional. | **Corrected:** EP-0002 now names `implemented-in: v0.80.0`; EP-0001 requires release metadata for every Implemented Standards EP; and `TestImplementedStandardsEPsNameRelease` enforces the rule repository-wide. The same sweep proves every Accepted/Partial Standards EP remains explicitly linked from PLAN. |
| C92 | The first real official-supervise setup PTY exposed several cross-boundary assumptions hidden by isolated tests: a first-action choice was covered by the landing view; a typed-nil or failed optional retained bridge removed ordinary Fleet; a child AgentLoop published application turn facts with no admitted generation; periodic application recovery was gated on native verification work; `agent.down` conflated the Fleet control handle with the child git-session coordinate; and omitted spawn `execution` remained empty instead of its documented `wait` default. | **Corrected:** the landing predicate accounts for an active choice; Fleet access returns literal nil and optional retained setup cannot disable ordinary spawning; child loops suppress application publication without an exact admitted generation; every successful lifecycle poll ticks the serialized application; terminal facts carry separate `agent_id` and `session_id`; and the Fleet bridge normalizes and validates execution before admission. Focused/race regressions cover each generic boundary, while an isolated ephemeral-key PTY builds and installs the official source, cancels first-action setup, then completes default setup through a fresh baseline proposal and operator rejection without activating recurrence. Later PTY paths cover exact review, immutable operator-input routing, pivot confirmation, verification/completion, reload/restart, and compaction; repetition against the real offline-key-signed package remains a release gate. |
| C93 | Confirming the official baseline and cancelling its active recurrence exposed three boundaries not exercised by unit-only mocks: lifecycle exports returned meaningful negative error payloads that the host collapsed to “invalid length”; wazero's deterministic default WASI clock made near-future durable timers appear years outside the broker horizon; and the plugin required a separate terminal control sequence for an application-owned WorkerRun cancellation that is itself the WAL transition. | **Corrected:** lifecycle calls decode bounded UTF-8 negative error payloads; persistent plugin modules observe host wall and monotonic clocks while broker deadline validation remains authoritative; and cancelled WorkerRuns use their own WAL sequence and reject a fabricated control sequence. A cross-repository ephemeral-key PTY now confirms the baseline, starts the exact WorkerRun, cancels it while the provider turn is live, and recovers its versioned terminal state through `/supervise status`. Focused host/plugin regressions and the plugin's full check remain green. Later PTY paths cover exact review, immutable operator-input routing, pivot confirmation, verification/completion, reload/restart, and compaction; repetition against the real offline-key-signed package remains a release gate. |
| C94 | C93 proved that the broker WorkerRun became terminal, but the application command result had no typed way to tell the TUI to stop the already-running local provider/tool turn. Durable cancellation could therefore coexist with an orphaned in-flight request until that request ended independently. | **Corrected:** `CommandResult.cancel_worker_run_id` is mutually exclusive with start/resume handoffs and carries no authority or expected version. The host re-reads the exact run through the admitted application's controller, accepts only `cancelled`, refuses a conflicting local owner, then cancels the matching provider/tool context, clears its pending calls and recurrence, and reconciles captured operator input. Unit/race tests freeze terminal and fail-closed paths; the signed-development PTY holds the worker HTTP stream and observes its request context close after `/supervise cancel`. |
| C95 | The signed `provider:invoke:<tokens>` capability was described as a hard cumulative input/output ceiling, but an adapter could ignore `MaxTokens`, report an overrun only at the terminal event, or omit a tokenizer so a permissive heuristic under-reserved the request. Completed over-budget text could therefore cross the WASM ABI. | **Corrected:** the host computes a conservative structural one-byte-per-token input bound and refuses before dispatch when it consumes the signed ceiling, reserves the bounded request plus requested output, and commits actual reported/estimated usage even on failure. The native bridge cancels on a reported overrun; a final defense-in-depth check converts every over-ceiling result to `failed/token_budget` with empty text. The official `llm-invoke` consumer and bundled auto-compact independently reject over-ceiling facts and bound the returned buffer. No-tokenizer pre-dispatch, reported-overrun, host-accounting, and consumer regressions freeze the hard token-only contract. |
| C96 | The official registry application's package load/unload operation applies every same-source tool atomically and supports a 4096-tool catalog, while the host surface-edit import rejected more than 64 names. Any package with 65 or more tools therefore could not use the signed whole-package workflow, and chunking would violate atomicity. | **Corrected:** catalog pages remain bounded at 64, but one strict surface-edit request may carry up to 4096 names under the existing 1 MiB request ceiling. The host validates the entire unique/non-overlapping/known/allowed/digest-current batch before one atomic controller edit; 4096 succeeds and 4097 or an oversized request changes nothing. The official registry schemas/parser and a 65-tool package regression use the same limit. |
| C97 | CLI and TUI selected a tool from a session-local registry but resolved its installed manifest/path through one process-global name map that was reset and repopulated non-atomically. Concurrent registries with different roots could transiently miss or cross-wire the same model-facing name to another signed package. | **Corrected:** `runtime.InstalledModuleForTool` accepts the exact registry-selected adapter and returns the immutable manifest, loader-authenticated identity, and WASM path carried by that object. CLI and TUI pass the selected adapter; the global map, reset, and name-only lookup are deleted. Concurrent isolated-registry and child-only/name-stand-in regressions prove object selection cannot acquire another registry's module authority. |
| C98 | A child provider failure published `agent.down` with the host-collected terminal usage/cleanup facts but returned a zero `subagent.Result` alongside the error. Fleet retained that zero result, so a later `agent:read` disagreed with the durable terminal event and a correctly strict application could only fail closed. | **Corrected:** every post-child-start error path constructs one terminal result carrying the same child identity, status/error, messages, and measured terminal metadata used for `agent.down`; Fleet retains that result even when the call also returns an error. Parent-cancellation and cleanup regressions compare the synchronous result and terminal event exactly. |
| C99 | After a watchdog pause had already terminalized a WorkerRun as `interrupted`, `/supervise cancel` completed application cleanup but still returned `cancel_worker_run_id`. The native host correctly requires that handoff to project `cancelled`, so it surfaced a false handoff error after a successful terminal cleanup. | **Corrected in the official application:** cancellation returns the native handoff ID only when the application itself performed and durably observed the broker `cancelled` transition. Already interrupted/stopped terminal runs are cleaned without mislabelling their projection; reply-loss replay of a true cancellation still returns the exact ID. The signed-development PTY crosses an anchorless turn, exact progress claim, fresh pinned reviewer, current-anchor approval, scheduling barrier, 1/1 advancement, and clean terminal cancellation. |
| C100 | A structured pivot attached its exact replacement only to durable `PendingReview` after the initial `start_review` action had already copied a pivot-less request. The first spawn and replay therefore reused one idempotency key with different prompts, which the host correctly rejected. The same live path built a recommendation panel ID by prefixing the full SHA-256 digest and exceeded the 64-byte UI ABI bound. | **Corrected in the official application:** deterministic review construction attaches the exact current-anchor pivot before both durable state and the start action are cloned; a regression byte-compares the complete initial and replayed spawn requests. The panel ID is a bounded deterministic digest-derived identifier with an explicit host-limit regression. An ephemeral-key PTY proves the worker can only request, a fresh pinned watchdog can only recommend, the operator confirms the exact proposal, the artifact CAS re-anchors the plan, revised recurrence sees that anchor, and cancellation closes it. |
| C101 | The first live Verify Work-to-completion path exposed four cross-boundary mismatches hidden by isolated tests: a published turn event did not consume its application iteration when an immediately returned schedule pause made the command fail; native verification compared audit-successor commit identity instead of source content and conflated the broker lifecycle session ID with the logical Git subject; a verification cancelled after worker terminalization requested a redundant pause that survived resume; and the official application strictly decoded only three fields from the broker's full authenticated `Completion` fact. | **Corrected:** every successfully published boundary consumes its sequence before schedule handling, while unpublished failures do not; verification resolves the exact source commit and compares its content tree with the current tree, permits audit-only successor commits, rejects changed content, and validates the logical subject only through the canonical turn ref while broker scope remains controller-authenticated; `cancelled/worker_terminal` releases the quality hold without manufacturing another pause; and the application strictly consumes and validates the complete session/generation/plugin/evidence/continuation/WAL/time completion response. Focused regressions freeze every invariant and reject unknown completion fields. A real ephemeral-sign/install PTY now crosses the operator-configured suite, exact evidence facts, watchdog review, fresh independent verifier, durable successful-completion handoff, and exact hold release. |
| C102 | Durable logical-session adoption keyed the broker scope to `Model.cwd`. A first TUI launched inside the checkout while `stado session resume` launched inside the detached worktree, so the same exact Git subject presented a different cwd; the broker correctly rejected that credential and the host silently created a fresh empty application scope. | **Corrected:** the TUI derives the durable admission cwd from the session sidecar's canonical user-repository root, which is stable across initial launch, switching, reload, and explicit resume; the detached worktree remains the execution path and is never misrepresented as the authority root. Focused transition tests prove first launch and resume present the same subject/cwd tuple. A live ephemeral-sign/install PTY pauses the exact supervise WorkerRun, reloads transactionally, exits, resumes the same Git session in a new process, re-adopts the same broker journal/run projection, proves no interrupted recurrence was duplicated, and cleans up terminally. |
| C103 | Automatic context recovery still refused an application-owned WorkerRun under a stale pre-C35 guard. Even if admitted, the handoff path would replay its prompt as ordinary input, matched the auto-compact display name instead of its authenticated canonical identity, and left a ghost recurrence when recovery timed out. | **Corrected:** overflow records whether the exact local recurrence is application-owned, binds the loaded plugin's canonical identity, transfers the complete broker scope through the authenticated direct-child handoff, and reconciles the broker-active WorkerRun once in the child instead of ordinary replay. Failed recovery restores the blocked prompt and durably cancels the source run. Focused/race regressions freeze identity, replay, timeout, and terminal cleanup; a live ephemeral-sign/install PTY proves overflow, compacted-child handoff, application rebind, child-anchored progress review, interrupted status, and cleanup without duplicate or bypass recurrence. |
| C75 | `plugin doctor` treats most lifecycle/artifact/agent/timer/UI application capabilities as unknown and recommends `stado run`, even though C26 deliberately makes the interactive TUI the only v0.80 lifecycle-application host and `run` rejects configured applications. | **Corrected:** doctor recognizes artifact, scoped session, agent, timer, lifecycle, and UI bridge capabilities. Any lifecycle manifest receives a dedicated compatibility report with interactive TUI as its only supported host; `run`, headless/ACP, and `tool run` are explicitly refused. |
| C76 | Source-keyed install records and plugin locks describe identity but are unkeyed files, including inside an operator-enabled project `.stado`. Installed-package admission can accept any globally trusted signer for a fabricated namespace with no package floor, and legacy floor migration can then persist the claim. | **Corrected:** user-state receipts bind canonical install root plus the StoreKey that hashes complete provenance. Every runtime admission requires that receipt; remote admission additionally requires the exact accepted owner signer. Legacy floor minting is deleted. A fabricated project record/lock around a valid globally trusted signature fails without the host receipt and still fails without the owner binding. The explicitly documented same-UID state-record limitation remains. |
| C77 | Trust-store and plugin-lock updates use atomic file replacement but unsynchronized read-modify-write. Concurrent installs, verification-floor changes, or anchor updates can each commit a valid file while silently losing the other operation's signer floor, owner pin, or lock row. | **Corrected:** Linux no-follow flock files serialize complete trust, receipt, and lock RMW transactions; each rereads under lock and retains atomic replacement. Remote continuity is authoritatively rechecked inside the same lock callback as row insertion. Barriered cross-process floor/lock-row tests and a conflicting-tag regression prove updates cannot be lost or simultaneously admitted. |

This table is intentionally a queue, not proof that every proposed correction
is right. Each item must be checked against code, history, and the owning EP
before changing behavior or metadata.

## Ordered implementation queue

[The root PLAN](../../../PLAN.md#current-state-after-v0802) is the sole
authoritative work and release sequence. This ledger is a decision and
inconsistency index only; it deliberately does not duplicate an order that can
stale. The v0.80.1 terminal repeat found and corrected C34's path-dependent
official build; v0.80.2's validation and release gates are complete, and PLAN
carries the ordered post-release work.

## External plugin source and signing checkpoint

`github.com/foobarto/stado-plugins` is the source and release home for official
application plugins, including supervise. EP-42's existing in-tree bundled
source rule continues to govern the small first-party embedded primitive/tool
set; it does not move official application policy back into stado core.
Official `supervise/v0.1.1` source, signed Git commits/tag, and release artifacts
are remote and reproducible; the temporary checkout is not authoritative and
may be discarded. The manifest signing key remains outside both repositories
and must never be searched, copied, displayed, or bypassed. Development or
ephemeral signatures remain test-only and may not be presented as the official
plugin identity.
