{{- /* User messages render as full-width cards so chat history stays
       easy to scan even in long technical sessions. */ -}}
{{ railCard .Body .Width "role_user" }}
{{- if .Queued }}
{{ color "muted" "  ⋯ queued — runs when the current turn finishes" }}
{{- end }}
